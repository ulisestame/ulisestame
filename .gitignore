# Only README.md should be in the public repo.
# Whitelist pattern: everything is ignored by default.
# To track a new file, add an explicit !filename entry below.

*

# Explicitly allowed (tracked in the public repo)
!.gitignore
!README.md
